# Example environment configuration for Notes Frontend (React)
# Copy to .env and adjust values as needed for your environment.

# Use HTTPS with backend if applicable! (Mandatory for production on Kavia cloud and often in CI.)
REACT_APP_BACKEND_URL=https://vscode-internal-595-beta.beta01.cloud.kavia.ai:3001
